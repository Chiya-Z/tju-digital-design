仿照 UCB 的 venus，实现 TEMU 仿真器的图形界面。

回答如下：

当前需求完整性评分为 4/10 分，“仿照 UCB Venus”的目标方向明确，但交付形态/范围/约束不足，无法进入方案设计。

1. GUI 形态希望是哪种？（本地浏览器 Web 页 / Electron 桌面 / Qt 等原生 GUI）
2. 最小必须功能有哪些？（Run/Step/Reset/Stop/加载程序/断点/监视点/寄存器/内存/反汇编/控制台输出 等）
3. 程序加载方式希望如何做？（继续用 inst.bin/data.bin 文件；或在 GUI 中上传/选择；是否需要“重新汇编”功能）
4. 前后端通信方式偏好？（TEMU 进程内嵌 HTTP/WebSocket；或外部桥接进程与 TEMU 通过 stdin/stdout 交互）
5. 是否允许引入新依赖与构建链？（Node.js/前端框架；或要求纯 C/最少依赖）

1. “Prev” 的语义是什么？（回退 1 条指令的寄存器+内存完整状态 / 仅回退 PC / 不需要 Prev 但保留按钮）
2. “从编辑器重新汇编”是否可假设本机已安装 loongarch32r-linux-gnusf-* 交叉工具链？（是/否；若否需改为仅支持上传 inst.bin/data.bin）
3. 反汇编/机器码展示的数据来源希望是什么？（直接读取 *.asm / 运行时动态反汇编 / 两者都要）