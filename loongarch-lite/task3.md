在 TEMU/temu 目录的 trace 目录下生成指令执行的 golden trace，记录除了分支和 Store 指令外每条指令的 PC 和写寄存器的信息：

1. 在框架代码的基础上，实现一个新的模块。
2. 第一列为 PC，第二列为寄存器的编号，第三列为待写入寄存器的值。第一行为表头。 
3. 不需要包含分支指令和 Store 指令。
4. PC 和寄存器值均以十六进制 32 bit 表示，不需要前导 0x，寄存器编号以十进制表示，寄存器编号为个位数时，前面补零对齐。
5. 注意模块化实现。
6. 当前环境无法运行仿真器，所以不要尝试运行代码，只需实现指令的功能即可。
